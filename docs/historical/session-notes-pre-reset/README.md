# Pre-Reset Session Notes

These files are preserved copies of loose top-level `Ceal/session_notes/` artifacts rescued on April 16, 2026.

They are kept separately from `docs/session_notes/` because they belong to the pre-reset / pre-reconciliation history and do not all match the canonical session-note set already tracked in repo.

Rescue rule used for this folder:

- Preserve files that are unique or materially different from the in-repo `docs/session_notes/` collection.
- Do not duplicate a file when the top-level version is byte-for-byte equivalent to an existing canonical in-repo note.

Intentional omission:

- `SESSION_NOTES_2026-04-02_continuation.md` was not copied here because it already matches `docs/session_notes/2026-04-02_multi-ai-onboarding.md`.
